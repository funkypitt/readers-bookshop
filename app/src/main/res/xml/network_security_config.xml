<?xml version="1.0" encoding="utf-8"?>
<!-- Every catalogue is reached over HTTPS. Plain HTTP is never allowed. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
